import 'package:flutter/material.dart';
class BillGates extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("BillGates"),),
    );
  }
}